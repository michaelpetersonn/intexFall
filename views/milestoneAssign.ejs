<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Assign Milestones - Ella Rises</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body { background: #fff7f4; font-family: 'Montserrat', sans-serif; color: #1f1f1f; }
    h1, h2, h3, h4, h5, h6 { font-family: 'DM Serif Display', serif; color: #1f1f1f; }
    .btn-primary { background-color: #9AB59D; border-color: #9AB59D; color: #0f1c10; }
    .btn-primary:hover { background-color: #839c85; border-color: #839c85; color: #0f1c10; }
    .btn-success { background-color: #F9AFB1; border-color: #F9AFB1; color: #1f1f1f; }
    .btn-success:hover { background-color: #f3898e; border-color: #f3898e; color: #1f1f1f; }
    .card { border: none; border-radius: 12px; box-shadow: 0 6px 18px rgba(0,0,0,0.08); }
    .table > :not(caption) > * > * { background-color: #fff; }
  </style>
</head>
<body class="bg-light">
  <div class="container py-4">

    <!-- Header -->
    <header class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">Assign Milestone</h1>
      <div>
        <a href="/milestones?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
           class="btn btn-outline-secondary btn-sm">
          Back to milestones
        </a>
      </div>
    </header>

    <div class="card shadow-sm">
      <div class="card-body">
        <form method="POST"
              action="/milestones/assign?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
              class="row g-3">

          <!-- Participant dropdown -->
          <div class="col-md-6">
            <label class="form-label">Participant</label>
            <select name="participant_email" class="form-select" required>
              <option value="">Select participant...</option>
              <% participants.forEach(p => { %>
                <option value="<%= p.participant_email %>">
                  <%= p.participant_last_name %>, <%= p.participant_first_name %>
                  (<%= p.participant_email %>)
                </option>
              <% }) %>
            </select>
          </div>

          <!-- Milestone title text input -->
          <div class="col-md-6">
            <label class="form-label">Milestone title</label>
            <input name="milestone_title"
                   class="form-control"
                   placeholder="e.g., Completed Resume Workshop"
                   required>
          </div>

          <div class="col-12">
            <button class="btn btn-dark" type="submit">Assign milestone</button>
          </div>

        </form>
      </div>
    </div>

  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Event Instance</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body { background: #fff7f4; font-family: 'Montserrat', sans-serif; color: #1f1f1f; }
    h1, h2, h3, h4, h5, h6 { font-family: 'DM Serif Display', serif; color: #1f1f1f; }
    .btn-primary { background-color: #9AB59D; border-color: #9AB59D; color: #0f1c10; }
    .btn-primary:hover { background-color: #839c85; border-color: #839c85; color: #0f1c10; }
    .btn-success { background-color: #F9AFB1; border-color: #F9AFB1; color: #1f1f1f; }
    .btn-success:hover { background-color: #f3898e; border-color: #f3898e; color: #1f1f1f; }
    .card { border: none; border-radius: 12px; box-shadow: 0 6px 18px rgba(0,0,0,0.08); }
    .ribbon {
      background: linear-gradient(90deg, #FFD8D1, #F9AFB1);
      padding: 12px 16px;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.08);
    }
  </style>
</head>
<body class="bg-light">
<div class="container my-4">
  <header class="ribbon d-flex flex-column flex-lg-row align-items-lg-center gap-2 mb-3">
    <div class="d-flex align-items-center gap-2">
      <img src="/images/logo.png" alt="Ella Rises logo" style="height:56px; width:auto;">
      <h1 class="h4 mb-0">Edit Event Instance</h1>
    </div>
    <div class="ms-lg-auto d-flex align-items-center gap-2 flex-wrap">
      <small class="me-2">Logged in as <strong>Manager</strong></small>
      <a href="/events?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>" class="btn btn-outline-secondary btn-sm">Back to events</a>
    </div>
  </header>

  <div class="card">
    <div class="card-body">
      <div class="mb-3">
        <h4 class="mb-1"><%= inst.event_name %></h4>
        <div class="text-muted">
          <%= inst.event_type || '' %>
          <% if (inst.event_description) { %> • <%= inst.event_description %><% } %>
        </div>
      </div>

      <form method="POST" action="/event_instances/edit/<%= inst.event_instance_id %>?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>" class="row g-3">
        <div class="col-md-6">
          <label class="form-label fw-semibold">Starts</label>
          <input type="datetime-local"
                 name="event_datetime_start"
                 class="form-control"
                 value="<%= new Date(inst.event_datetime_start).toISOString().slice(0,16) %>"
                 required>
        </div>
        <div class="col-md-6">
          <label class="form-label fw-semibold">Ends</label>
          <input type="datetime-local"
                 name="event_datetime_end"
                 class="form-control"
                 value="<%= new Date(inst.event_datetime_end).toISOString().slice(0,16) %>"
                 required>
        </div>

        <div class="col-md-6">
          <label class="form-label fw-semibold">Location</label>
          <input name="event_location" class="form-control" value="<%= inst.event_location || '' %>" placeholder="Location">
        </div>
        <div class="col-md-3">
          <label class="form-label fw-semibold">Capacity</label>
          <input name="event_capacity" type="number" min="0" class="form-control" value="<%= inst.event_capacity != null ? inst.event_capacity : '' %>">
        </div>
        <div class="col-md-3">
          <label class="form-label fw-semibold">Registration deadline</label>
          <input type="datetime-local"
                 name="event_registration_deadline"
                 class="form-control"
                 value="<%= inst.event_registration_deadline ? new Date(inst.event_registration_deadline).toISOString().slice(0,16) : '' %>">
        </div>

        <div class="col-12 d-flex gap-2">
          <button class="btn btn-primary" type="submit">Save changes</button>
          <a class="btn btn-outline-secondary" href="/events?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Edit Milestone - Ella Rises</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">

    <header class="d-flex justify-content-between align-items-center mb-3">
      <div>
        <h1 class="h4 mb-0">Edit Milestone</h1>
        <small class="text-muted">
          Milestone ID: <%= milestone.milestone_id %>
        </small>
      </div>
      <div>
        <a href="/milestones?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
           class="btn btn-outline-secondary btn-sm">
          Back to milestones
        </a>
      </div>
    </header>

    <div class="card shadow-sm">
      <div class="card-body">

        <div class="mb-3">
          <label class="form-label">Participant</label>
          <p class="form-control-plaintext mb-0">
            <% if (milestone.participant_last_name || milestone.participant_first_name) { %>
              <strong><%= milestone.participant_last_name || '' %>, <%= milestone.participant_first_name || '' %></strong>
              (<%= milestone.participant_email %>)
            <% } else { %>
              <strong><%= milestone.participant_email %></strong>
            <% } %>
          </p>
        </div>

        <form method="POST"
              action="/milestones/edit/<%= milestone.milestone_id %>?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
              class="row g-3">

          <div class="col-md-8">
            <label class="form-label">Milestone title</label>
            <input name="milestone_title"
                   class="form-control"
                   value="<%= milestone.milestone_title %>"
                   required>
          </div>

          <div class="col-md-4">
            <label class="form-label">Date achieved</label>
            <input name="milestone_date"
                   type="date"
                   class="form-control"
                   value="<%= milestone.milestone_date || '' %>">
          </div>

          <div class="col-12 d-flex justify-content-between mt-3">
            <a href="/milestones?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
               class="btn btn-outline-secondary">
              Cancel
            </a>
            <button class="btn btn-primary" type="submit">Save changes</button>
          </div>

        </form>
      </div>
    </div>

  </div>
</body>
</html>

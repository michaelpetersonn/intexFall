<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Add Participant - Ella Rises</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">
    <header class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">Add Participant</h1>
      <div>
        <a href="/participants?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
           class="btn btn-outline-secondary btn-sm">
          Back to participants
        </a>
      </div>
    </header>

    <div class="card shadow-sm">
      <div class="card-body">
        <form method="POST"
              action="/participants/add?userId=<%= loggedInUserId %>&level=<%= loggedInLevel %>"
              class="row g-3">

          <div class="col-md-6">
            <label class="form-label">Email (login key)</label>
            <input name="participant_email" type="email" class="form-control" required>
          </div>

          <div class="col-md-3">
            <label class="form-label">First name</label>
            <input name="participant_first_name" class="form-control" required>
          </div>

          <div class="col-md-3">
            <label class="form-label">Last name</label>
            <input name="participant_last_name" class="form-control" required>
          </div>

          <div class="col-md-3">
            <label class="form-label">Role</label>
            <input name="participant_role" class="form-control" placeholder="e.g. Student">
          </div>

          <div class="col-md-3">
            <label class="form-label">Date of Birth</label>
            <input name="participant_dob" type="date" class="form-control">
          </div>

          <div class="col-md-3">
            <label class="form-label">Phone (10 digits)</label>
            <input name="participant_phone" class="form-control" placeholder="8015551234">
          </div>

          <div class="col-md-3">
            <label class="form-label">Field of Interest</label>
            <input name="participant_field_of_interest" class="form-control">
          </div>

          <div class="col-md-3">
            <label class="form-label">City</label>
            <input name="participant_city" class="form-control">
          </div>

          <div class="col-md-3">
            <label class="form-label">State</label>
            <input name="participant_state" class="form-control">
          </div>

          <div class="col-md-3">
            <label class="form-label">Zip</label>
            <input name="participant_zip" class="form-control">
          </div>

          <div class="col-md-6">
            <label class="form-label">School / Employer</label>
            <input name="participant_school_or_employer" class="form-control">
          </div>

          <div class="col-12">
            <button class="btn btn-dark" type="submit">Save participant</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
